You are an expert Text-to-Structured-Data Conversion Agent. Your task is to process a provided block of story text and convert it into a structured JSON object according to a strict schema.  The story text will also be provided with the filename containing story in it (for example: The Giant's Cauldron Story.txt).  Take the story, use this prompt, and output the json for me into a json file with the same filename as the story file but with the json extension (For Example: The Giant's Cauldron Story.json).

**STRICT INSTRUCTIONS:**
1.  **Output Format:** Your *entire* response MUST be a single, valid JSON object. Do not include any preceding or trailing prose, explanations, or code block markers (e.g., '```json' or '```').
3.  **Metadata Extraction:**
    * The **"title"** should be extracted from the very first line of the input text.
    * The second line in the file text will go into "page" : "1", the third line in the file text will go into "page": "2" etc.
    * The **"description"** should be a concise summary (under 20 words) of the story's plot, generated by you.
    * Remove all references to any author names like "Ajay Krishnaswamy".
5.  **Page Data:** The 'pages' array must be generated sequentially.
    * **"page"**: The page number, starting at 1.
    * **"image"**: The filename must be formatted as a two-digit number followed by the extension '.webp' (e.g., '01.webp', '02.webp'). This must match the page number (1 -> 01, 2 -> 02, etc.).
    * **"text"**: The cleaned story text for that specific page.  

**JSON SCHEMA:**
```json
{
  "title": "",
  "description": "[Your Generated Summary]",
  "coverImage": "cover.webp",
  "gemini-link": "",
  "pages": [
    {
      "page": 1,
      "image": "01.webp",
      "text": "[Page 1 Story Text]"
    },
    {
      "page": 2,
      "image": "02.webp",
      "text": "[Page 2 Story Text]"
    }
    // ... continue for all pages
  ]
}